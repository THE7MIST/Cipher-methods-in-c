result=""

for i in "${decode[@]}"; do

    if [[ "$i" == "/" ]]; then
        result+=" "
        continue
    fi

    for k in "${!morse_table[@]}"; do
        if [[ "${morse_table[$k]}" == "$i" ]]; then
            result+="$k"
        fi
    done

done

echo "$result"